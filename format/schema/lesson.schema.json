{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Lesson frontmatter (course format v1)",
  "description": "Frontmatter for a lesson page (course/NNN-slug/NN-slug/index.md): the page fields, plus a stable id. Renderer-specific keys (layout, hero, ...) are not allowed.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "title"],
  "properties": {
    "id": {
      "description": "Stable identifier, unique within its course. Never change it after publishing, even if the folder or title changes.",
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 }
  }
}
