{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Page frontmatter (course format v1)",
  "description": "Frontmatter for every published page. Renderer-specific keys (layout, hero, ...) are not allowed.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title"],
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 }
  }
}
