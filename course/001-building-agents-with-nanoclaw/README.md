# Building community agents with NanoClaw: contributor notes

This README is for people who maintain this course. It is not published. Learners read [index.md](./index.md), which is published at https://aith.aijutsu.dev/001-building-agents-with-nanoclaw/.

- `index.md` is the course page: the objectives, the list of lessons, and the References table. Each lesson is a folder with its own `index.md`: `01-installations/` and `02-setting-up-nanoclaw/`. Both are split into sub-lessons. Images sit in an `img/` folder next to the page that uses them, named for what they show (`img/botfather-mini-app-home.png`). `course.yaml` is the course manifest. Both follow the [course format](../../docs/system/course-format.md).
- `01-installations/` (titled "Getting Started") is split into sub-lessons: `01-terminal/`, then an app store per system — `02-homebrew/` (macOS and Linux, and inside Ubuntu on Windows) and `03-chocolatey/` (Windows only) — then one per tool: `04-git/`, `05-docker/`, `06-make/`, `07-codex/` and the optional `08-claude-code/`. The two app-store pages carry their system in the title, because each reader does one and skips the other. Its own `index.md` is the overview (the pre-requisites, and a table of what each tool is for). Keep that table and the sub-lesson folders in step: adding or removing a tool means both. Sub-lesson pages are three levels under `course/`, so glossary links are `../../../glossary.md#term`.
- `02-setting-up-nanoclaw/` is split into sub-lessons, one per step: `01-telegram-bot/`, `02-download-nanoclaw/` and `03-run-setup/`. Its own `index.md` is the overview (the fork note, and a table of the steps). Keep that table and the sub-lesson folders in step. Where a step can be done in more than one way — the BotFather mini app or the chat, the Notion page or the developer portal — the ways are `<details>` sections of one group, not headings, so only one is open at a time. `03-run-setup/` has none: it has one path, run in the terminal. It ends with "Say hi to your agent", which used to be a fourth sub-lesson (`04-say-hi/`, id `say-hi`): setup already shows the first Telegram message, so replying to it is the same moment, not a page of its own.
- `03-setting-up-data-sources/` gives the agent somewhere to keep what it learns: `01-notion-page/` (duplicate the published Louis template), `02-notion-connection/` (an internal connection and `make add-notion-connection`), `03-connect-notion-page/` (give that connection access to the copy) and `04-watch-it-fill/` (send Louis the link to that copy, answer onboarding, record a member). Page access is its own page because a valid token with no access is the failure that reads as a broken agent — `connecting-notion.md` calls it the step most setups miss. It follows the `community-assistant` template's own setup references, so keep the two in step. **The agent is called Louis** (`agentName` in the template's `plugin.json`), and the pages say so.
- `04-agent-playtime/` is where the agent meets other people: `01-create-telegram-group/` (make the group, add the bot), `02-add-agent-to-group/` (the mention, the owner's approval card, then a Notion read/write round trip), `03-add-humans-to-group/` (membership tiers and verification, from the template's `permissions.md`) and `04-update-personality/` (ask the agent, or edit its standing instructions with Codex). Pictures that are still to come are marked with an invisible `<!-- screenshot: … -->` comment, so a page stays publishable while it waits; `grep -rn "screenshot:" course/` lists what is outstanding.
- `05-cleaning-up/` is the last lesson: how to take the course back off a computer. Today it has one sub-lesson, `01-uninstalling-nanoclaw/`. Anything else the course installs later (a second channel) gets a page here when it leaves something behind. Its overview says the lesson is skippable while the reader is still using NanoClaw, so nobody walks into it by accident. It was `03-cleaning-up/` until the data-sources lesson went in front of it; the folder number changed, the lesson `id` (`cleaning-up`) did not, which is what ids are for.
- **Every install page follows the same shape.** Inside each operating system's `<details>`, in this order:
  1. **Check if it's already installed.** — one command, then what each result means. If the tool is there, it says where to skip to.
  2. **Install it.** — the steps.
  3. **Check that it works.** — one command, and what the reader should see. Troubleshooting for that system only.

  Write these three as **bold labels**, never as `###` headings. VitePress's `outline: 'deep'` would put every heading inside a `<details>` into the page's "On this page" list, so the reader would see macOS, Windows and Linux copies of the same three entries.

  Where the two checks would be the same command (Git on Windows and Linux, Make everywhere), the first one says "skip the rest of this section" instead of sending the reader to the second one to run it again. Where they differ (Docker: `docker --version` then `docker run hello-world`; Codex: `codex --version` then `codex login status`), the first routes to the second.

  **Every command block says where to type it**, in the line right above it: "In Terminal, run:", "In the Ubuntu terminal, run:", "In the terminal, run:". The word links to `01-terminal/` once per section — the first command in each `<details>`, and the first in the page's own prose — so a reader who opens one system's section always gets the link without the same href appearing ten times on a page. `make validate` can't check this; the audit is a one-off script over the fenced blocks.

  Two exceptions to watch. On Windows, `wsl --install` runs in **PowerShell**, not the Ubuntu terminal the rest of the course uses, so those blocks say PowerShell and carry no link. On `01-terminal/` itself nothing links to `01-terminal/`: its checks say "the Terminal window that just opened".

  Three pages vary, on purpose. `01-terminal/` uses **Open it.** instead of **Install it.** on macOS and Linux, where nothing is installed and the first check just says so. `07-codex/` and `08-claude-code/` have no `<details>` at all: one `brew install --cask` covers every system, so their labels are `##` headings, and each page says why it has no sections per system. `03-chocolatey/` has none either: it is one system.

- **`08-claude-code/` is optional, and its title says so** ("Install Claude Code (optional)"). It is an alternative to Codex as the reader's helper for setup, not an extra requirement: the agent runs on Codex either way. Keep three things in step if this changes: the "Should you install it?" section on that page (including the line that says skipping it changes nothing), the line in `02-setting-up-nanoclaw/03-run-setup/index.md` that says setup can open Codex or Claude Code when it fails, and the optional Claude-plan line in the `01-installations/index.md` pre-requisites.
- `nanoclaw/` is our fork of NanoClaw, pinned as a submodule. We customised it with NanoClaw's own `/add-codex` and `/add-telegram` skills. The fork lives at https://github.com/aijutsu/aith-nanoclaw-codex-telegram. See `AGENTS.md` → Fork submodules.
- To move the fork to a newer NanoClaw version, use this repository's `update-submodule` skill. It runs NanoClaw's own `/update-nanoclaw` skill for you, and then checks the course against the new version.

## What the install steps depend on

The steps in the lessons — `01-installations/` (its overview and its eight sub-lessons), `02-setting-up-nanoclaw/`, `03-setting-up-data-sources/`, `04-agent-playtime/` and `05-cleaning-up/` — describe what the pinned fork does. They were checked against the fork at `48d7260a` on 2026-09-24. When the pin moves, check these again (the `update-submodule` skill asks you to re-check every course instruction):

| Course step | Depends on (paths inside `nanoclaw/`) |
| --- | --- |
| Supported systems (macOS, Linux, Windows only through WSL2), in `01-installations/index.md` | `README.md` → Requirements |
| `01-installations/04-git/`: install Git and jq | The Telegram token check pipes `getMe` through `jq` (`.claude/skills/add-telegram/SKILL.md`). Setup never installs `jq`. |
| `01-installations/05-docker/`: install Docker; on Windows, Docker Desktop with WSL integration | `setup/container.ts`, `setup/install-docker.sh`. If `docker` is missing, setup installs Docker Engine itself (on WSL, inside Ubuntu, which we don't want). |
| `01-installations/05-docker/`: on a Mac, OrbStack (recommended) or Docker Desktop, running before setup | `setup/container.ts`: when Docker isn't running, setup runs `open -a Docker`, which starts Docker Desktop but not OrbStack. When `docker` is missing, `setup/install-docker.sh` installs Docker Desktop. |
| `01-installations/06-make/`: install Make | The fork's `Makefile` (added with the `add-notion-credentials` skill). `03-setting-up-data-sources/02-notion-connection/` runs `make add-notion-connection`, so the Make page now pays for itself. |
| `01-installations/07-codex/`: install Codex before setup | `setup/providers/codex.ts`: ChatGPT sign-in needs the `codex` command, and setup exits without it. |
| `01-installations/07-codex/`: `codex login status` says `Logged in using ChatGPT` | The Codex CLI, not the fork. Checked against `codex-cli 0.154.0` on 2026-09-23. If OpenAI changes that subcommand or its wording, the page's last check breaks. `setup/providers/codex.ts` reads `~/.codex/auth.json` instead, so setup keeps working either way. |
| `01-installations/08-claude-code/`: `claude auth status --text` | Claude Code, not the fork. Checked against `2.1.272 (Claude Code)` on 2026-09-23; `--text` prints `Login method:` and the plan name (the default is JSON, which is too noisy for the page). The fork only needs `claude` to exist and be signed in (`setup/lib/claude-handoff.ts`). |
| `01-installations/07-codex/` and `08-claude-code/`: `brew install --cask codex` and `brew install --cask claude-code@latest` | Homebrew, not the fork. Both casks ship Linux builds (`x86_64_linux`, `arm64_linux`, `depends_on: {}`), which is why one command covers macOS, Linux and Ubuntu on Windows. Checked 2026-09-24: `codex` 0.156.1, `claude-code@latest` 2.1.281. **There is no `codex@latest`**; plain `claude-code` tracks a slower release line, so the `@latest` suffix is load-bearing. Re-check with `brew info --cask <name>` when either page is reviewed. |
| `01-installations/02-homebrew/`: Homebrew inside Ubuntu on Windows | Homebrew supports WSL 2 and calls WSL 1 a Tier 3 platform with known problems running installed executables (docs.brew.sh/Homebrew-on-Linux). That is why `01-terminal/` now tells a Windows reader to check the **VERSION** column of `wsl -l -v` and, if it says `1`, run `wsl --set-version Ubuntu 2`. Keep those two pages in step. |
| `01-installations/03-chocolatey/`: `choco install docker-desktop` | The Chocolatey community package, not Docker's own installer, so `05-docker/` keeps the manual download as a fallback. The install one-liner is quoted from docs.chocolatey.org/en-us/choco/setup, checked 2026-09-24. |
| The version strings quoted in every "Check that it works" (`git version 2.55.0`, `jq-1.7.1`, `Docker version 29.4.0, build 9d7ad9f`, `GNU Make 3.81`/`4.3`, `codex-cli 0.154.0`) | Nothing in the fork. They are examples, and each page says the reader's number may differ. Refresh them if they get old enough to look wrong. |
| Run setup yourself, not inside Codex | `nanoclaw.sh` and `setup/auto.ts` read answers from the terminal (`/dev/tty`) and hand it to `codex login`. |
| What setup offers when it fails | `setup/lib/claude-handoff.ts` (`offerClaudeOnFailure`), `setup/providers/codex.ts` (`offerCodexFailureAssist`). Before the runtime is picked, failures go to Claude ("Want to debug this with Claude?", or "Claude CLI is needed to diagnose this. Install it now?" when it isn't installed). After Codex is picked, setup offers "Want to debug this with Codex?" first, but only if `codex` runs and `~/.codex/auth.json` exists. It falls back to Claude only if Claude is installed and signed in. |
| `--agent-provider codex` | `setup/lib/setup-config.ts` (the flag), `setup/auto.ts` (it skips the runtime picker and the pre-built image offer). |
| The table of setup questions | The prompts in `setup/auto.ts`, `setup/providers/codex.ts`, `setup/channels/`, and `.claude/skills/add-telegram/SKILL.md`. Quote them exactly. |
| The answers that pick a template: **From local templates**, then **`community-assistant`** | The option at `setup/auto.ts:1011` (`local`, hinted "templates/ in this install"), `pickLocalTemplate()`, and `chooseTemplate()` (`:1098`). The template is **in the fork**, at `templates/community/community-assistant/` (pinned, so every reader gets it). The picker's label is the last part of the ref (`setup/templates.ts:147`), which is why the answer is `community-assistant` and not `community/community-assistant`. Upstream ships `templates/` empty, so this row depends on the fork's own commit, not on NanoClaw. |
| `04-agent-playtime/02-add-agent-to-group/`: the mention, then the card in the owner's DM | `src/router.ts` auto-creates a messaging group only on a mention, then escalates through `channelRequestGate` when nothing is wired. The card's title (`📣 Bot mentioned in new channel`) and its options (`Connect to <agent>`, `Choose existing agent`, `Connect new agent`, `Reject`) are in `src/modules/permissions/channel-approval.ts` (`buildApprovalOptions`). **`Connect new agent` creates an empty agent group** (`createNewAgentGroup`) — it does not offer a template, which is why the course tells the reader to pick Louis. |
| `04-agent-playtime/03-add-humans-to-group/`: tiers, the matrix, DM-only verification, the append-only Admin Log | `templates/community/community-assistant/skills/community-assistant/references/permissions.md`. Identity is the platform user ID via `Telegram Accounts` → `Member`; the first member ever created is the Admin and everyone after starts `New`. |
| `04-agent-playtime/04-update-personality/`: ask the agent, or edit its standing instructions | `container/skills/self-customize/SKILL.md`: memory and `instructions.prepend.md` are the agent's own to edit, with no approval, while the composed `CLAUDE.md`/`AGENTS.md` is rebuilt every spawn and must not be edited (`src/project-doc-compose.ts`). |
| The Common issues table | `setup/auto.ts:594` (the test-agent cleanup warning). There is no OneCLI row any more: since 2.4.0, OneCLI installs through the `add-onecli` skill, whose `scripts/setup.ts` reuses a healthy existing OneCLI without asking. |
| `03-setting-up-data-sources/`: one parent page, a connection shared with it, everything inherited | `templates/community/community-assistant/skills/community-assistant/references/connecting-notion.md` (*Sharing pages with the integration*). |
| The **sixteen** databases, and that the reader duplicates a template rather than the agent building anything | `…/references/notion-schema.md` (the list and the themes) and `…/references/community-onboarding.md` (*Bind to their copy of the template*): "You never create these databases. They arrive with the copy." The course must never tell a reader to build them, nor let the agent search by title. |
| The published Louis template URL in `01-notion-page/` | **Not in the fork, and not pinned.** The template README says the URL is handed over out of band and no plugin file contains it. The course carries the copy Aijutsu published: https://app.notion.com/p/Louis-the-Community-Builder-3e4f2896ef0c80988978ee9724337b93. If that page moves or is republished, this course page breaks and nothing here will catch it. |
| `02-notion-connection/`: `make add-notion-connection` | The `add-notion-credentials` skill and the fork's `Makefile` (`scripts/notion-connection.ts`). It exists because Notion is not in OneCLI's app catalogue, so there is no Apps → Connect flow, and the hand-rolled secret needs four flags to be exactly right. Prefer it over the gateway link, whose `path=` must be blanked by hand. |
| The token goes to the credential keeper, never to the agent | `mcp.json` ships `NOTION_TOKEN: "placeholder"`; the OneCLI vault swaps in the real value on the way out (`docs/v1-to-v2-changes.md`, `http://127.0.0.1:10254`). `connecting-notion.md` tells the agent never to accept a credential value in chat, which is why the course tells the reader the agent won't ask. |
| The first Notion call failing with `401`, and the agent handing over a connect link | `connecting-notion.md`, *What "not connected" looks like* and *Connect it*. The same table is the source for the course's troubleshooting rows (`object_not_found`, `restricted_resource`). |
| **Notion's own UI wording** — the developer portal, **Internal connections**, **Create a new connection**, the **Configuration** tab, **Installation access token** (`ntn_`), **Content access** → **Edit access**, and **•••** → **Connections** → **+ Add connection** | Not the fork, and not pinned: https://developers.notion.com/guides/get-started/internal-connections, checked 2026-09-23. The template's own `connecting-notion.md` still says `notion.so/profile/integrations` and "Internal Integration Token", which is the older naming — the agent is told to expect drift, the course pages are not. Re-check the course pages when Notion moves things. |
| BotFather steps and Group Privacy | `.claude/skills/add-telegram/SKILL.md`, and https://core.telegram.org/bots/features |
| Don't move the folder after setup | `src/install-slug.ts`: the service name is made from the folder path. |
| Private chats answer every message; groups need a mention, or a reply to one of the bot's messages; answers quote the message they answer and open the asker's reply (force-reply) | `src/channels/telegram.ts`; replies, quoting and force-reply come from the fork's `telegram-reply-threading` skill (`src/channels/telegram-reply-aware.ts`, upgraded in place by `src/channels/chat-sdk-bridge.ts`), not upstream |
| `05-cleaning-up/01-uninstalling-nanoclaw/`: `./uninstall.sh`, and `-n` for a preview | `uninstall.sh` (a shim that maps `-n`/`-y` to `--dry-run`/`--yes` and execs `nanoclaw.sh --uninstall`), and the `--uninstall` short-circuit in `nanoclaw.sh`, which runs before bootstrap so uninstalling never installs anything first. Both files are mode `755` in Git, so `./uninstall.sh` works straight after a clone. |
| The three groups the uninstaller asks about, and what is in each | `GROUPS` in `setup/uninstall/flow.ts`, and the `what:` labels in `setup/uninstall/scan.ts`. Quote the group titles exactly. |
| Enter keeps, `y` deletes; nothing is deleted until every question is answered; Ctrl-C is safe | `confirmGroup()` (`initialValue: false`) and the confirm phase in `setup/uninstall/flow.ts`. |
| `.env` is copied to `.env.bak` **in the same folder**, so it dies with the folder | `backupEnv()` in `setup/uninstall/remove.ts`. On a name clash it writes `.env.bak.<timestamp>` instead. |
| The uninstaller leaves the checkout folder, and the "What is left on your computer" list | `printLeftAlone()` in `setup/uninstall/flow.ts`, and the uninstall FAQ in the fork's `README.md`. |
| `Can't run the uninstaller: dependencies are missing (node_modules/).` | The fallback branch in `nanoclaw.sh`, which prints manual cleanup commands and exits 1. |
